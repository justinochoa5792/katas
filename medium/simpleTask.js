// A Simple Task
// Mubashir needs your help in a simple task.
// Create a function that takes a number n and return its decimal part.

function decimalPart(n) {
  return (decimals = +n.toString().replace(/^[^\.]+/, "0"));
}
